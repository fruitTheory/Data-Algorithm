#pragma once

#include <vector>

using std::vector;

template <typename T>
class Heap
{
private:

public:
Heap();
~Heap();
};

template <typename T>
Heap<T>::Heap(){

}

template <typename T>
Heap<T>::~Heap(){

}
